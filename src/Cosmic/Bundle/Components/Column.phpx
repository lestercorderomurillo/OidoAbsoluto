<?php

namespace Cosmic\Bundle\Components;

use Cosmic\Binder\Component;

class Column extends Component
{
    public function __construct($size = "auto", $mediumSize = 12, $smallSize = 12, int $padding = 2, string $textPosition = "center")
    {
        $this->mediumSize = $mediumSize;
        $this->smallSize = $smallSize;
        $this->padding = $padding;
        $this->size = $size;
        $this->textPosition = $textPosition;
    }

    public function render()
    {
        if($this->size == "auto"){
            return {{
                <div class="col col-auto text-{textPosition} mx-auto p-{padding} {class}">
                    {body}
                </div>
            }};
        }

        return {{
            <div class="col col-{smallSize} col-sm-{smallSize} col-md-{mediumSize} col-lg-{size} col-xl-{size} text-{textPosition} p-{padding} mx-auto {class}">
                {body}
            </div>
        }};
    }
}

publish(Column::class);
